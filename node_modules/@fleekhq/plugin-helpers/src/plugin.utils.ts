export const getPackageData = (packageJSON: any) => {
  const [, name] = packageJSON.name.split('/');

  const packageData = {
    name,
    packageName: packageJSON.name,
    author: packageJSON.author,
    version: packageJSON.version,
    description: packageJSON.description,
  };

  return packageData;
};
